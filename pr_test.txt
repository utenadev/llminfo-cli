## Summary

This PR implements several features based on code review feedback:

## Changes

### Caching
- Add `CacheManager` class for 1-hour model cache by provider
- Cache location: `~/.cache/llminfo/`
- Add `--force` option to `list models` command to bypass cache
- All providers use cache by default

### Validation
- Add `validators.py` with provider configuration validation
- Validate required fields, base_url format, parser type, api_key_env case
- Add validation to `test-provider` and `import-provider` commands

### CI/CD
- Add GitHub Actions workflows:
  - `ci.yml` - Build and test on push/PR to main/develop
  - `pr.yml` - Run tests and auto-comment on PR failure
  - Check coverage threshold (80%)
  - Auto-run: pytest, ruff check, ruff format, mypy
  - Add pip dependency caching

### Documentation Updates
- Update README.md: Add Caching section, update Features section
- Update README.ja.md: Add Caching section, update Features section
- Update BLUEPRINT.md: Add cache.py, validators.py, test files to project structure
- Update SPEC.md: Add validation, cache functionality details

### Bug Fixes
- Fix README.md to remove duplicate and unimplemented sections
- Improve cache invalidation error handling (OSError catch)
- Add pytest noqa comments to suppress unused import warnings

## Testing

- All tests pass: 15 passed, 4 skipped
- Ruff: All checks passed
- Mypy: no issues found

## Checklist

- [x] Tests pass
- [x] Ruff checks pass
- [x] Mypy checks pass
- [x] Documentation updated
- [x] CI/CD workflows added
